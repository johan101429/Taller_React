import React from 'react';
import Timer from '../../components/Timer/Timer';

const TimerPage = () => {
    return (
        <div>
            <h2>Timer Page</h2>
            <Timer />
        </div>
    );
};

export default TimerPage;
